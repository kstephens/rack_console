require 'spec_helper'

describe RackConsole do
  it 'has a version number' do
    expect(RackConsole::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
